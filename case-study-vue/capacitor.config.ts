import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'case--study-vue',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
